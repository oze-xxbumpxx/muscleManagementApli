# Preferences

## 2026-02-28

- 入力値の方針: 空文字は基本的に許容しない。
- 主要項目（例: `exerciseName`）は空文字禁止で統一する。
- 必要時のみ空文字を `null` に正規化する（通常は不正入力として扱う）。
- リファクタ記録: `exerciseNames` 取得処理の型堅牢性向上（`raw: true` 利用時の型ガード追加または `raw: false` への見直し）は後続で対応する。

