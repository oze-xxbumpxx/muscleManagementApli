query TrainingSessions($limit: Int, $offset: Int) {
  trainingSessions(limit: $limit, offset: $offset) {
    totalCount
    items {
      id
      date
      bodyWeight
      notes
      exercises {
        id
        exerciseName
        order
        sets
        reps
        durationSeconds
        weight
      }
    }
  }
}

query TrainingSession($id: Int!) {
  trainingSession(id: $id) {
    id
    date
    bodyWeight
    notes
    exercises {
      id
      exerciseName
      order
      sets
      reps
      durationSeconds
      weight
      notes
    }
  }
}

mutation CreateTrainingSession($input: CreateTrainingSessionInput!) {
  createTrainingSession(input: $input) {
    id
    date
  }
}

mutation UpdateTrainingSession($id: Int!, $input: UpdateTrainingSessionInput!) {
  updateTrainingSession(id: $id, input: $input) {
    id
    date
    bodyWeight
    notes
  }
}
